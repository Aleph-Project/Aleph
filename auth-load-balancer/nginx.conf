events {}


http {
    upstream auth_service {
        least_conn;
        server auth-ms:4000;
        server auth-ms-a:4000;
        server auth-ms-b:4000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://auth_service;
        }
    }
}